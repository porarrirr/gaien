import Foundation
import SwiftUI

// タイマー画面はアプリ共通のデザインシステム（AppColors / cardStyle）に統一したため、
// 旧来の独自テーマ（TimerAmbientTheme / TimerAmbientBackgroundView）は廃止した。
// 背景は各画面側の AppColors.subtleBackground を使用する。
